{
    "configurations": [
    {
        "name": "Launch Integrated Browser",
        "request": "launch",
        "type": "editor-browser",
        "url": "http://localhost:8080",
        "webRoot": "${workspaceFolder}"
    }
    ]
}